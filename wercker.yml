box: node
build:
  steps:
    - script:
        name: modular-engine lib build
        code: |
          npm i
          npm run build
test:
  steps:
    - script:
        name: modular-engine lib tests
        code: |
          npm i
          npm run test